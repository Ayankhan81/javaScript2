# Function :-
function is block of code that is written once 
and can be used many times to perform a specfic task.
it is used do some work and it runs only when it is called.

function parameter are local variable means it only accessible in block scope

# Arrow Function


 <!-- Important question -->
 /**
  * for each is (higher order function/method )
  * hof is a method that either take another function as an parameter or they
  * return another function as an output 
  */










# DOM:- Document Object Model
when a web page is loaded , the browser creates a DOM of the page.

console.log() :- ye print krane ke kaam aata h
console.dir() :- ye special object ki properties ya method ko print krane ke kaam aata h


it is method of accessing html code in javaScript


if we write script tag before the body the dom object will not be accesible that is the major reason why we write script tag just before the closing body tag